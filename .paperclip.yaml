schema: paperclip/v1
agents:
  ceo:
    adapter:
      type: opencode_local
      config:
        model: omniroute/auto/best-free
  sales-lead:
    adapter:
      type: opencode_local
      config:
        model: omniroute/auto/best-free
  deals-lead:
    adapter:
      type: opencode_local
      config:
        model: omniroute/auto/best-free
  marketing-lead:
    adapter:
      type: opencode_local
      config:
        model: omniroute/auto/best-free
  operations-lead:
    adapter:
      type: opencode_local
      config:
        model: omniroute/auto/best-free
  intelligence-lead:
    adapter:
      type: opencode_local
      config:
        model: omniroute/auto/best-free
  customer-lead:
    adapter:
      type: opencode_local
      config:
        model: omniroute/auto/best-free
  backoffice-lead:
    adapter:
      type: opencode_local
      config:
        model: omniroute/auto/best-free
  hermes-operator:
    adapter:
      type: hermes_gateway
      config:
        apiBaseUrl: http://172.23.80.1:<HERMES_API_PORT>
        sessionKeyStrategy: issue
        # apiKey: Hermes API_SERVER_KEY - set in the Paperclip UI after import (secret, do not commit)
